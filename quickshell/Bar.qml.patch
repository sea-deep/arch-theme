--- quickshell/Bar.qml
+++ quickshell/Bar.qml
@@ -37,2 +37,8 @@
     margins.right: Theme.outerGap
+
+    onActiveChanged: {
+        if (!active) {
+            UiState.closeOverlays()
+        }
+    }
 
